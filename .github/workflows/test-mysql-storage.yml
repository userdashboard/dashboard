name: test-mysql-storage

on: [push]

jobs:
  test-mysql-storage:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.15.1]
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - uses: mirromutth/mysql-action@v1.1
      with:
        mysql database: dashboard
        mysql user: username
        mysql password: password
    - name: Set up Chrome
      run: |
        if [ ! -z "$PUPPETEER_SKIP_CHROMIUM_DOWNLOAD" ]; then
          APT_INSTALL="chromium gconf-service libasound2 libatk1.0-0 libc6 libcairo2 libcups2 libdbus-1-3 libexpat1 libfontconfig1 libgcc1 libgconf-2-4 libgdk-pixbuf2.0-0 libglib2.0-0 libgtk-3-0 libnspr4 libpango-1.0-0 libpangocairo-1.0-0 libstdc++6 libx11-6 libx11-xcb1 libxcb1 libxcomposite1 libxcursor1 libxdamage1 libxext6 libxfixes3 libxi6 libxrandr2 libxrender1 libxss1 libxtst6 ca-certificates fonts-liberation libappindicator1 libnss3 lsb-release xdg-utils wget"
          if [ ! -z "$APT_PROXY" ]; then
            echo "deb $APT_PROXY buster main" > /etc/apt/sources.list
          fi
          apt-get update
          apt-get install -y $APT_INSTALL
        fi
    - name: Set up project
      run: |
        HERE=`pwd`
        mkdir /tmp/project
        cd /tmp/project
        if [ ! -z "$NPM_PROXY" ]; then
          echo registry=$NPM_PROXY > .npmrc
        fi
        npm init -y
        npm install "$HERE" @userdashboard/storage-mysql mocha puppeteer faker
        echo "global.applicationPath = __dirname" > main.js
    - name: Test unencrypted MySQL storage
      run: ./node_modules/.bin/mocha --retries 2 --file main.js --bail --exit --full-trace --recursive --timeout 90000000 "./node_modules/@userdashboard/**/{,!(public)}/*.test.js"
      working-directory: "/tmp/project"
      env:
        NODE_ENV: testing
        STORAGE: "@userdashboard/storage-mysql"
        DATABASE_URL: "mysql://username:password@localhost:3306/dashboard"
        NODE_OPTIONS: "--preserve-symlinks"
    - name: Test encrypted MySQL storage
      run: ./node_modules/.bin/mocha --retries 2 --file main.js --bail --exit --full-trace --recursive --timeout 90000000 "./node_modules/@userdashboard/**/{,!(public)}/*.test.js"
      working-directory: "/tmp/project"
      env:
        NODE_ENV: testing
        STORAGE: "@userdashboard/storage-mysql"
        DATABASE_URL: "mysql://username:password@localhost:3306/dashboard"
        ENCRYPTION_SECRET: "12345678901234567890123456789012"
        ENCRYPTION_SECRET_IV: "1234123412341234"
        NODE_OPTIONS: "--preserve-symlinks"